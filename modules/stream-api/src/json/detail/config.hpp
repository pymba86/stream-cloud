#pragma once

namespace stream_cloud {
    namespace api {
        namespace json {
                template <typename T>
                struct config;
                struct config_tag
                { };
        }
    }
}